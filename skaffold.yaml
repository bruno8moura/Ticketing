apiVersion: skaffold/v2beta9
kind: Config
metadata:
  name: ticketing
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
build:
  artifacts:
  - image: brunnouw/auth
    context: auth
    docker:
      dockerfile: Dockerfile
    sync:
      manual:
        - src: 'src/**/*.ts'
          dest: .
  - image: brunnouw/client
    context: client
    docker:
      dockerfile: Dockerfile
    sync:
      manual:
        - src: '**/*.js'
          dest: .
  - image: brunnouw/tickets
    context: tickets
    docker:
      dockerfile: Dockerfile
    sync:
      manual:
        - src: 'src/**/*.ts'
          dest: .